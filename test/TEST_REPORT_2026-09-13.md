# 单元测试报告 · iLive Admin CRUD

> 自动生成 · generated by `test/run-tests.mjs`

## 概要 · summary

| 项目 | 值 |
|------|-----|
| **测试状态** | ✅ PASS（全部通过） |
| **数据日期** | 2026-09-13 |
| **生成时间** | 2026-09-13T12:20:20.364Z |
| **用例总数** | 58 |
| **通过** | 58 |
| **失败** | 0 |
| **通过率** | 100.0% |
| **被测模块** | `db.mjs`、`crud.mjs`、`server.mjs` |
| **测试文件** | `test/crud.test.mjs`、`test/api.test.mjs` |
| **测试案例** | `test/fixtures/*.json` |
| **数据库文件** | `F:\static\ilive_admin\db\data.db` |

## 覆盖范围 · coverage

- **db.mjs**：`DB_PATH` / `SCHEMA_PATH`、建表幂等、外键开启、单例
- **crud.mjs**：表配置与 schema 一致性、i18n JSON 校验与压缩、
  必填与类型校验、整型/布尔转换、外键与唯一约束的友好报错、
  单行表限制、分页与默认排序、全字段搜索 `q`、部分更新、级联删除统计
- **server.mjs**：页面与元信息接口、增删改查全链路、分页、
  全字段搜索 `q`、400 / 404 / 405 错误分支

## 失败明细 · failures

无 · none

## 用例清单 · test cases

| # | 结果 | 用例 |
|---|------|------|
| 1 | ✅ | GET / 返回管理页面 |
| 2 | ✅ | GET /api/tables 返回 10 张表与数据库路径 |
| 3 | ✅ | 新增 → 列表 → 详情 → 编辑 → 删除 完整链路 |
| 4 | ✅ | PATCH 与 PUT 等效 |
| 5 | ✅ | 列表支持 limit 与 offset 分页 |
| 6 | ✅ | 列表支持 q 全字段搜索（含 i18n JSON） |
| 7 | ✅ | 删除演唱会返回级联清理统计 |
| 8 | ✅ | 详情：记录不存在返回 404 |
| 9 | ✅ | 新增：缺必填返回 400 与友好提示 |
| 10 | ✅ | 新增：非法 JSON 请求体返回 400 |
| 11 | ✅ | 新增：i18n 字段非法 JSON 返回 400 |
| 12 | ✅ | 新增：外键不存在返回 400 |
| 13 | ✅ | 新增：唯一约束冲突返回 400 |
| 14 | ✅ | 未知表返回 400 |
| 15 | ✅ | 不支持的方法返回 405（DELETE 缺主键） |
| 16 | ✅ | 未知路径返回 404 |
| 17 | ✅ | TABLES 覆盖 schema.sql 中的全部表 |
| 18 | ✅ | listTables 返回字段 / 必填 / JSON 字段等元信息 |
| 19 | ✅ | 每张表的每个字段都有非空中文标题 |
| 20 | ✅ | fields 与 fieldLabels 键序一致且无重复 |
| 21 | ✅ | listTables 暴露 fieldLabels 供界面显示中文标题 |
| 22 | ✅ | DB_PATH 采用环境变量指定的临时库 |
| 23 | ✅ | SCHEMA_PATH 指向 db/schema.sql 且文件存在 |
| 24 | ✅ | initSchema 幂等：重复执行不报错、种子不重复 |
| 25 | ✅ | 外键约束已开启（保证级联删除生效） |
| 26 | ✅ | getDb 返回同一实例（单例） |
| 27 | ✅ | 新增城市：i18n 对象写入并返回自增主键 |
| 28 | ✅ | 新增演唱会（完整案例）：多语言字段可完整读回 |
| 29 | ✅ | 新增标签（数组式 i18n）：各语言数组正常存储 |
| 30 | ✅ | 新增友情链接：enabled 与多语言字段正常 |
| 31 | ✅ | 新增：i18n 传 JSON 字符串会被解析并压缩存储 |
| 32 | ✅ | 新增：非法 JSON 抛错 |
| 33 | ✅ | 新增：缺必填字段抛错（案例 city-missing-required.json） |
| 34 | ✅ | 新增：必填字段为空串抛错 |
| 35 | ✅ | 新增：整数字段字符串转数字、布尔转 0/1 |
| 36 | ✅ | 新增：非整数值写入整数字段抛错 |
| 37 | ✅ | 新增：可选字段空串存为 NULL |
| 38 | ✅ | 新增：外键不存在给出友好错误 |
| 39 | ✅ | 新增：唯一约束冲突给出友好错误 |
| 40 | ✅ | 新增：单行表 site_settings 已有记录时被拒 |
| 41 | ✅ | 新增：未知表抛错 |
| 42 | ✅ | listRows 分页：total / limit / offset 与默认排序 |
| 43 | ✅ | listRows 全字段搜索 q：匹配文本列与 i18n JSON、空结果、空 q 等价于无过滤 |
| 44 | ✅ | getRow：存在返回记录，不存在返回 null |
| 45 | ✅ | getRow：文本主键表 site_seo_i18n 可正常读取 |
| 46 | ✅ | 种子数据已按 schema.sql 写入 |
| 47 | ✅ | updateRow：部分更新不影响其他字段 |
| 48 | ✅ | updateRow：空对象原样返回 |
| 49 | ✅ | updateRow：主键不可修改（传入被忽略） |
| 50 | ✅ | updateRow：更新 i18n 字段 |
| 51 | ✅ | updateRow：记录不存在抛错 |
| 52 | ✅ | updateRow：必填字段置空抛错 |
| 53 | ✅ | updateRow：可选字段可置为 NULL |
| 54 | ✅ | deleteRow：返回删除条数与被删记录 |
| 55 | ✅ | deleteRow：记录不存在抛错 |
| 56 | ✅ | deleteRow：删除演唱会级联清理 4 张子表 |
| 57 | ✅ | deleteRow：演唱会无子表时 cascade 全为 0 |
| 58 | ✅ | countChildren：无子表的表返回空对象 |

## 运行方式 · how to run

```bash
node test/run-tests.mjs                  # 跑全部测试并生成本报告
node --test test/crud.test.mjs           # 仅数据层单元测试
node --test test/api.test.mjs            # 仅 REST API 集成测试
```

# 依赖 · Dependencies
node_modules/

# 构建产物 · Build output
.nuxt/
.output/
dist/

# 本地数据库 · Local database (generated by db:seed)
# 方案 C：data.db 需随代码包部署到 Vercel，故不忽略 *.db
# (plan C: data.db must be committed to be deployed read-only)
#*.db
#*.db-*
#*.db-wal
#*.db-shm

# 环境变量 · Env (secrets) —— 切勿提交真实 token
.env
.env.local
.env.production
.env.*.local
# 模板文件保留在仓库（无密钥）· Keep template (no secrets) in repo
!.env.example

# 日志 · Logs
*.log
npm-debug.log*

# 系统文件 · OS/editor files
.DS_Store
Thumbs.db


# 开发工具
.trae/
.workbuddy/
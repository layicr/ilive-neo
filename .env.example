# Turso / LibSQL 环境变量（不入版本库）· Turso / LibSQL env (not committed)
# 本地：SQLite 文件协议，免注册 · Local: file: protocol, no signup needed
TURSO_DATABASE_URL=file:./public/data/data.db
# 远程 Turso 时才需要 · Only required for remote Turso
TURSO_AUTH_TOKEN=